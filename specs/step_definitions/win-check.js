import { Given, When, Then } from "@badeball/cypress-cucumber-preprocessor";

/* No duplicate steps, this one already in replay-button.js
Given('the game is loaded', () => {});*/

/* No duplicate steps, this one already in replay-button.js
Given('the player enters {string} as the red player', (a) => {});*/

/* No duplicate steps, this one already in replay-button.js
Given('the player enters {string} as the yellow player', (a) => {});*/

When('the following moves are made:', () => {
  // TODO: implement step
});

Then('the system should declare {string} as the winner', (a) => {
  // TODO: implement step
});

Then('the {string} button should be visible', (a) => {
  // TODO: implement step
});

/* No duplicate steps, this one already above
When('the following moves are made:', () => {});*/

/* No duplicate steps, this one already above
Then('the system should declare {string} as the winner', (a) => {});*/

/* No duplicate steps, this one already above
Then('the {string} button should be visible', (a) => {});*/

/* No duplicate steps, this one already above
When('the following moves are made:', () => {});*/

/* No duplicate steps, this one already above
Then('the system should declare {string} as the winner', (a) => {});*/

/* No duplicate steps, this one already above
Then('the {string} button should be visible', (a) => {});*/